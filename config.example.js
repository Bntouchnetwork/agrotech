/*
 * AgroTech — Configuração de ambiente (MODELO)
 * ------------------------------------------------------------------
 * Copie este arquivo para "config.js" e coloque a sua chave real.
 *
 *     cp config.example.js config.js
 *
 * O arquivo config.js está no .gitignore e NÃO deve ser commitado.
 * A segurança da chave no front-end é garantida pela restrição de
 * HTTP Referrer configurada no Google Cloud (não por ocultá-la).
 *
 * >>> INSIRA SUA API KEY NO ARQUIVO config.js, NÃO AQUI. <<<
 */
window.AGROTECH_CONFIG = {
  // Cole aqui a sua chave da Google Maps JavaScript API (somente no config.js)
  GOOGLE_MAPS_API_KEY: "COLE_SUA_CHAVE_AQUI",

  // Map ID (Cloud-based map styling). Já preenchido para a AgroTech.
  GOOGLE_MAPS_MAP_ID: "f651f614f590ba96d783db3e"
};
